{
  "name": "qx-engine",
  "version": "2.0.0",
  "private": true,
  "description": "QX candle-reaction signal engine (Python): Quotex live tick feed via session token + Yahoo real-market fallback, tick-level candle analysis, confluence signals, backtest. NO simulation.",
  "scripts": {
    "dev": "bash ./run.sh",
    "start": "bash ./run.sh"
  }
}
