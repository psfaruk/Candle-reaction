# QX ক্যান্ডেল রিয়েকশন সিগন্যাল ইঞ্জিন — Railway ডিপ্লয় গাইড

## ✨ এখন সম্পূর্ণ ZERO-CONFIG

এই রিপো এখন **কোনো ম্যানুয়াল সেটআপ ছাড়াই** Railway-তে চলে:

- **PORT অটো** — Railway যে পোর্ট দেবে, ইঞ্জিন সেটাতেই বাইন্ড করবে (`0.0.0.0`)
- **`railway.json` রিপোতে আছে** — Dockerfile বিল্ডার + `/qx-health` হেলথচেক অটো-কনফিগ
- **ডেটাবেস অটো** — `/data` (ভলিউম) লেখার মতো হলে সেটাই ব্যবহার হবে, না হলে অটো `/app/db` ফলব্যাক — কোনো env ভ্যারিয়েবল লাগবে না
- **Schema অটো** — ইঞ্জিন বুটের সময় নিজেই `CREATE TABLE IF NOT EXISTS` চালিয়ে খালি DB হলেও নিজে ঠিক করে নেয় (prisma db push-ও চলে, ৩ বার রিট্রাই সহ — কিন্তু সেটা ফেইল করলেও অ্যাপ চলবে)
- **Self-healing** — supervisor স্ক্রিপ্ট কোনো প্রসেস মারা গেলে ৫ সেকেন্ডে অটো-রিস্টার্ট করে
- **Next.js standalone node দিয়ে চলে** (bun নয়) — প্রোডাকশনে সবচেয়ে নির্ভরযোগ্য পথ

## ডিপ্লয় ধাপ (মাত্র ৩ ধাপ)

1. **GitHub-এ পুশ করুন** (ইতিমধ্যে করা আছে: `psfaruk/Candle-reaction`)

2. **Railway-তে নতুন প্রজেক্ট**: [railway.app](https://railway.app) → **New Project** → **Deploy from GitHub repo** → `Candle-reaction` রিপো সিলেক্ট → **Deploy**।
   `railway.json` থাকায় Railway সরাসরি Dockerfile দিয়ে বিল্ড করবে (৩–৫ মিনিট)।

3. **ডোমেইন চালু করুন**: Service → **Settings** → **Networking** → **Generate Domain** → সাইট খুলুন।
   সবকিছু অটো: হোম/সিগন্যাল/সেটিংস ট্যাব, লাইভ মার্কেট, ব্যাকটেস্ট — সব চলবে।

### ঐচ্ছিক (পার্সিস্টেন্স + অটো-লাইভের জন্য)

- **ভলিউম যোগ করুন** (রিডিপ্লয়ে সিগন্যাল হিস্ট্রি রাখতে): Service → **Settings** → **Volumes** → **Add Volume** → Mount path: `/data`
  - ভলিউম না দিলেও অ্যাপ চলবে — শুধু রিডিপ্লয়/রিস্টার্টে হিস্ট্রি মুছে যাবে
- **QX_TOKEN ভ্যারিয়েবল (অটো-সেটআপ ✨)**: Service → **Variables** → `QX_TOKEN` = আপনার QX টোকেন (authorization লাইন বা q9securid — দুটোই কাজ করবে)
  - সেট করলে **টোকেন পেস্ট করতেই হবে না** — প্রতি রিস্টার্টে/রিডিপ্লয়ে ইঞ্জিন নিজে থেকেই Quotex লাইভ কানেক্ট করবে
  - সেটিংস ট্যাবে নীল ব্যাজ দেখাবে: "⚙ QX_TOKEN ভ্যারিয়েবল সক্রিয়"
  - ভ্যারিয়েবল বদলালে সার্ভিস অটো-রিস্টার্ট হয় → নতুন টোকেন সঙ্গে সঙ্গে কার্যকর
- **QX টোকেন কোথায় পাবেন (২ উপায়)**:
  1. **সবচেয়ে নির্ভরযোগ্য** — qxbroker.com-এ লগইন → **F12** → **Network** → **WS** ফিল্টার → socket.io/ws2 কানেকশন → **Messages** → সবুজ `42["authorization",{"session":"…"}]` লাইনটি **পুরোটা কপি** করুন
  2. **Application** → Cookies → `q9securid` ভ্যালু কপি করুন
  - পুরো authorization লাইন পেস্ট করলেও অ্যাপ নিজে টোকেন বের করে নেয়; টোকেন কয়েক ঘণ্টা/দিন পর **মেয়াদ শেষ হয়** — "প্রত্যাখ্যাত" দেখালে নতুন করে কপি করুন
- **QX টোকেন ম্যানুয়ালি**: সাইটের **সেটিংস ট্যাব** → টোকেন পেস্ট → **সংযোগ করুন** — ভার্ডিক্ট ৩–৫ সেকেন্ডেই আসে (লাইভ টিক / প্রত্যাখ্যাত / নেটওয়ার্ক-ব্লক)

> **অন্য কোনো ভ্যারিয়েবল লাগবে না** — PORT, ডেটাবেস, schema সব অটো।

### আগের ব্যর্থ ডিপ্লয় থেকে আপগ্রেড

যদি আগের ডিপ্লয়ে পেজ খোলে কিন্তু **বাটন/টোকেন কাজ না করে** (সকেট মৃত = ইঞ্জিন ওঠেনি — সাধারণত Nixpacks বিল্ডার শুধু Next.js চালিয়ে দেয়), এই নতুন কমিটে সেটা স্থায়ীভাবে ঠিক হয়েছে — `package.json`-এর start কমান্ড এখন সরাসরি ফুল-স্ট্যাক সুপারভাইজার চালায়, তাই **Nixpacks হোক বা Dockerfile — দুইভাবেই পুরো অ্যাপ ওঠে**। তবুও যদি সমস্যা থাকে:

1. এই নতুন কমিট পুশ হওয়ার পর Railway-এ সার্ভিসে **Redeploy** দিন, অথবা
2. Service → **Settings** → **Build** → Builder: **Dockerfile** সিলেক্ট করে আবার ডিপ্লয় করুন, অথবা
3. পুরনো সার্ভিস ডিলিট করে নতুন করে GitHub রিপো থেকে আবার কানেক্ট করুন (সবচেয়ে পরিষ্কার), অথবা
4. ব্রাউজারে হার্ড-রিফ্রেশ (Ctrl+Shift+R) — পুরনো JS ক্যাশে আটকে থাকতে পারে

**ডিপ্লয় ঠিক চলছে কিনা যাচাই**: `https://<আপনার-ডোমেইন>/qx-health` খুলুন —
`{"ok":true,"service":"qx-engine","mode":"SIM","pairs":8,...}` দেখালে কনটেইনার সুস্থ। মোড তখনই `LIVE` হবে যখন ভ্যালিড QX টোকেন (UI বা QX_TOKEN ভ্যারিয়েবল) কানেক্ট থাকবে। হেলথ OK কিন্তু বাটন মৃত = ব্রাউজার ক্যাশে পুরনো JS → হার্ড-রিফ্রেশ করুন।

## আর্কিটেকচার (এক পাবলিক পোর্ট)

```
browser ──▶ Railway $PORT
             └─ qx-engine (bun + socket.io)
                 ├─ /engine/*     → socket.io (লাইভ টিক, সিগন্যাল, RPC)
                 ├─ /qx-health    → হেলথচেক (200 JSON)
                 └─ বাকি সব       → proxy → Next.js standalone (:3001, node)
```

| প্রসেস | রানটাইম | পোর্ট | কাজ |
|---|---|---|---|
| qx-engine | bun | `$PORT` (পাবলিক) | Quotex লাইভ ফিড / সিমুলেটর, টিক→ক্যান্ডেল, সিগন্যাল, ব্যাকটেস্ট, SQLite, প্রক্সি |
| Next.js standalone | node | `3001` (ইন্টারনাল) | ফ্রন্টএন্ড UI |

কনটেইনার স্টার্টআপ (`deploy/start.sh`): DB পাথ অটো-ডিটেক্ট → Next (:3001) → Python engine ($PORT) → supervisor (মরলে অটো-রিস্টার্ট, SIGTERM-এ পরিষ্কার শাটডাউন)। Python ইঞ্জিন নিজেই SQLite স্কিমা বুটস্ট্র্যাপ করে — কোনো মাইগ্রেশন লাগে না।

## গুরুত্বপূর্ণ নোট

- **রেলওয়ের IP কি Quotex ব্লক করবে?** ws2.qxbroker.com স্যান্ডবক্স নেটওয়ার্ক থেকে সক্রিয়ভাবে কানেক্ট হয় (verified)। Railway IP ব্লক হলে অ্যাপ রিয়েল ইন্টারব্যাংক মার্কেট ফিডে (Yahoo) চলতে থাকবে — কোনো সিমুলেশন নেই। সমাধান:
  - **VPS অপশন**: Hetzner/Contabo VPS-এ একই Dockerfile: `docker build -t qx . && docker run -e PORT=8080 -v qxdata:/data -p 8080:8080 qx`
- **সিগন্যাল লজিক অপরিবর্তিত**: Quotex টিক হোক বা রিয়েল মার্কেট ফিড — কনফার্মেশন মডেল একই (লেভেল/জোন + স্ট্রাকচার + ফুল ক্লোজ + রিয়েকশন)। সিমুলেশন সম্পূর্ণ বাদ।
- **টোকেন নিরাপত্তা**: টোকেন সার্ভারের SQLite-এ সেভ হয়, ব্রাউজারে ফুল টোকেন কখনো ফেরত যায় না (মাস্কড)। যে কেউ URL জানলে ড্যাশবোর্ড দেখতে পারবে — ডোমেইন গোপন রাখুন।
- **ঝুঁকি সতর্কতা**: বাইনারি অপশন অত্যন্ত ঝুঁকিপূর্ণ। এই টুল টেকনিক্যাল অ্যানালাইসিস সাহায্যের জন্য; কোনো সিগন্যাল লাভের নিশ্চয়তা নয়।

## লোকাল রান

```bash
bun install && bun run db:push
bun run dev                                   # Next.js :3000 (টার্মিনাল ১)
cd mini-services/qx-engine && bun run dev     # ইঞ্জিন :3003 (টার্মিনাল ২)
```

ডকারে সম্পূর্ণ প্রোডাকশন-স্ট্যাক (এক পোর্টে সব):

```bash
docker build -t qx-signal .
docker run --rm -p 8080:8080 -e PORT=8080 -v qxdata:/data qx-signal
# খুলুন: http://localhost:8080  • হেলথ: http://localhost:8080/qx-health
```
